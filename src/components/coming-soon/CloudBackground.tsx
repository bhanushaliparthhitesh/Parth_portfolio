import styles from '@/app/coming-soon/coming.module.css';

export default function CloudBackground() {
  return (
    <div className={styles.skyBg}>
      {/* We are using CSS radial gradients to simulate the fluffy clouds */}
    </div>
  );
}
